My approach was to set up a minimal dev environment that connects to my mongo db instance running on localhost, create a database for the app, add a single record, and then scaffold a minimal express app. Then I implemented the app, up to the point where I could fetch the record from my browser. I then added an integration test suite that covers the requirements from the `Assignment` document to provide some confidence that it is working as intended.
